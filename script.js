

function show(num){
    document.getElementById("output").value += num;
    /* the += adds on the next number beside it*/
    
}

function clear(){
    document.getElementById("output").value = " ";
    /* retrieves the listed values in the output bar and sets it to empty string*/
}